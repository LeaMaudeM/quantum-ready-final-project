## 🧩 Getting Started

### 📌 Where to Get the Repository URL

1. Open the project on GitHub.
2. Click **Code** (green button).
3. Select **HTTPS**.
4. Click **Copy** to get the repository URL.

---

### 1️⃣ Clone the Repository

1. Accept the GitHub invitation.
2. Open an empty window in Visual Studio Code.
3. Click **“Clone a Repository”**.
4. Paste the repository URL.

---

### 2️⃣ Set Up the Environment

Create and activate a virtual environment:

```bash
python -m venv venv
venv\Scripts\activate   # Windows
```

Install dependencies:
pip install qiskit qiskit-aer

---

### 3️⃣ Run the project
python test_qiskit.py

---

## 🔁 Working with GitHub
Before Coding (Get Latest Changes)

If teammates pushed updates that you do not have:

git pull origin main

After Making Changes (Upload Your Work):

git add .
git commit -m "Your message"
git push
